# YOLOv8 航拍树木与石头检测——零基础中文使用手册

## 1. 这个程序能做什么

这个项目会完成两件事：

1. 用你现有的三套数据训练一个属于自己的 YOLOv8 模型。
2. 把一张新的航拍图片交给模型，输出带检测框的图片，并告诉你每个目标是树木还是石头。

第一版模型只有两个类别：

| 编号 | 英文名称 | 含义 |
| --- | --- | --- |
| 0 | `tree` | 树木、树冠，以及 Nature 数据集中的树干 |
| 1 | `stone` | 石头、岩块 |

## 2. 开始前要知道的几个词

- **数据集**：很多图片，以及告诉电脑“目标在哪里、是什么”的标签。
- **训练**：让模型反复查看数据集并调整内部参数。
- **权重**：训练成果，通常是一个 `.pt` 文件。
- **推理或预测**：用训练好的权重识别新图片。
- **epoch**：模型完整学习一遍训练集。100 epochs 就是完整学习 100 遍。
- **置信度**：模型对某个检测结果有多确定，越接近 1 越确定。

## 3. 已经为你配置好的路径

项目目录：

```text
D:\zyzcodex\codex-yolov8
```

三个原始数据集：

```text
F:\uav-program\训练数据集\Nature objects.v1i.yolov8
F:\uav-program\训练数据集\open-pit-rock-chunks-test.v3i.yolov8
F:\uav-program\训练数据集\Tree-Top-View.v1i.yolov8
```

合并后的派生数据默认保存到：

```text
F:\uav-program\训练数据集\uav-tree-stone.v1.yolov8
```

程序只读取三个原始数据集，不会修改它们。类别合并规则如下：

- Nature：`rock → stone`，`tree/trunk → tree`，忽略 `road/water`。
- Stone：`stone → stone`，多边形标签转换为普通检测框。
- Tree-Top-View：`tree-top → tree`。

如果以后移动了数据集，只需用记事本修改 `config\project.yaml` 中对应的 `root` 路径，不要修改 Python 源码。

## 4. 每次操作前都要做的两步

### 第一步：打开正确的命令窗口

推荐从 Windows 开始菜单打开 **Anaconda Prompt** 或 **Miniconda Prompt**。普通 CMD 也可以，但必须能识别 `conda` 命令。

### 第二步：激活环境并进入项目

逐行输入：

```bat
conda activate yolov8
cd /d D:\zyzcodex\codex-yolov8
```

看到命令行左边出现 `(yolov8)`，说明环境已经激活。

`cd /d` 的作用是同时切换磁盘和目录。路径中如果有空格或中文，始终使用英文双引号包住整个路径。

## 5. 检查运行环境

输入：

```bat
python check_environment.py
```

程序会显示：

- 当前是否在 `yolov8` 环境；
- Python、PyTorch、Ultralytics 版本；
- CUDA 是否可用；
- 显卡名称与计算能力；
- 一个非常小的 CUDA 运算是否成功。

理想结果包括：

```text
Conda 环境：yolov8
Python：3.8...
PyTorch：1.13.1+cu116
Ultralytics：8.4.112
CUDA 可用：是
显卡：NVIDIA GeForce RTX 4060 Laptop GPU
最小 CUDA 运算：通过
```

RTX 4060 的计算能力是 8.9，而当前 PyTorch 编译列表可能只直接显示到 `sm_86`。程序会给出警告，并以实际的小型 CUDA 运算结果为准。只要“最小 CUDA 运算：通过”，就可以继续。

## 6. 合并和检查数据集

确认三个原始数据集已经完整解压，然后运行：

```bat
python prepare_dataset.py --config config\project.yaml
```

程序会：

1. 检查三个数据集的 train、valid、test 目录；
2. 统一类别编号；
3. 把石头多边形转换为检测框；
4. 为文件名增加数据源前缀，避免同名文件互相覆盖；
5. 复制到新的派生数据目录；
6. 创建统一的 `data.yaml` 和 `dataset_manifest.json`。

成功后会看到每个划分的图片数、树木目标数和石头目标数。

### 如何确认结果

打开：

```text
F:\uav-program\训练数据集\uav-tree-stone.v1.yolov8
```

里面应当有：

```text
train\
valid\
test\
data.yaml
dataset_manifest.json
.uav_yolo_prepared.json
```

`dataset_manifest.json` 是统计清单；`.uav_yolo_prepared.json` 是安全标记。

### 想重新生成怎么办

第一次不要添加 `--overwrite`。只有确认旧的派生数据不再需要时，才运行：

```bat
python prepare_dataset.py --config config\project.yaml --overwrite
```

程序只允许覆盖带有安全标记的派生目录。如果目录不是本程序创建的，它会拒绝删除，防止误删文件。

## 7. 第一次训练模型

建议先做一次 5 轮的小规模流程检查：

```bat
python train.py --config config\project.yaml --epochs 5
```

这仍然是真实训练，只是轮数少，主要用来确认数据和显卡能正常工作。确认无误后，再进行正式训练：

```bat
python train.py --config config\project.yaml
```

默认设置是：

| 参数 | 默认值 | 解释 |
| --- | --- | --- |
| 模型 | `yolov8n.pt` | 体积最小的 YOLOv8 检测模型 |
| epochs | 100 | 完整学习数据 100 次 |
| imgsz | 640 | 输入图片缩放到 640 像素 |
| batch | 8 | 每次送入显卡 8 张图片 |
| device | 0 | 使用第 1 张 NVIDIA 显卡 |
| workers | 0 | Windows 下最稳妥的数据加载方式 |
| patience | 30 | 30 轮没有改善就提前停止 |

第一次使用 `yolov8n.pt` 时，Ultralytics 可能自动联网下载预训练权重。这个文件不来自清华 PyPI 镜像，所以下载速度取决于 GitHub 网络。

### 训练结果在哪里

通常保存在：

```text
D:\zyzcodex\codex-yolov8\runs\detect\uav_tree_stone
```

如果这个目录已经存在，Ultralytics 可能创建带数字的新目录。以训练开始时命令行显示的实际输出目录为准。

最重要的文件是：

```text
weights\best.pt
weights\last.pt
results.csv
results.png
confusion_matrix.png
```

- `best.pt`：验证效果最好的权重，平常检测图片优先使用它。
- `last.pt`：最后一轮的权重，训练意外中断后可以用它继续。

### 训练中断后继续

把路径换成实际的 `last.pt`：

```bat
python train.py --resume "D:\zyzcodex\codex-yolov8\runs\detect\uav_tree_stone\weights\last.pt"
```

### 显存不足怎么办

如果看到 `CUDA out of memory`，先减小 batch：

```bat
python train.py --config config\project.yaml --batch 4
```

还不行就改为：

```bat
python train.py --config config\project.yaml --batch 2
```

不要一开始就换更大的 `yolov8s.pt`、`yolov8m.pt`，因为它们会占用更多显存。

## 8. 怎么判断模型效果

训练目录中的图表会包含以下指标：

- **Precision（精确率）**：模型框出来的目标中，有多少是真的。
- **Recall（召回率）**：真实目标中，有多少被模型找到了。
- **mAP50**：在较宽松的重叠标准下综合评价检测效果。
- **mAP50-95**：在多种严格程度下的综合评价，比 mAP50 更严格。

不要只看训练集。验证集指标高、真实航拍图片效果也稳定，才说明模型有实际价值。

如果树木很多但石头效果差，应先检查石头数据数量、拍摄高度、光照、目标大小与真实无人机画面是否相似，而不是盲目增加 epochs。

## 9. 识别一张指定图片

假设最佳权重位于：

```text
D:\zyzcodex\codex-yolov8\runs\detect\uav_tree_stone\weights\best.pt
```

待检测图片位于：

```text
D:\待检测图片\example.jpg
```

运行：

```bat
python predict_image.py --model "D:\zyzcodex\codex-yolov8\runs\detect\uav_tree_stone\weights\best.pt" --source "D:\待检测图片\example.jpg"
```

结果默认保存到：

```text
D:\zyzcodex\codex-yolov8\outputs\predict
```

如果目录已经存在，程序会创建 `predict_2`、`predict_3`，不会覆盖以前的结果。

每个结果目录包含：

```text
images\               带检测框的图片
detections.json       程序易读取的结果
detections.csv        可用 Excel 打开的检测明细
```

CSV 中会记录类别、置信度和检测框左上角、右下角坐标。

## 10. 一次识别整个图片目录

把 `--source` 指向图片文件夹：

```bat
python predict_image.py --model "D:\zyzcodex\codex-yolov8\runs\detect\uav_tree_stone\weights\best.pt" --source "D:\待检测图片"
```

支持常见的 JPG、JPEG、PNG、BMP、TIF、TIFF 和 WEBP 图片。程序采用流式读取，不会一次把整个目录的图片都放进内存。

## 11. 调整检测严格程度

默认置信度阈值为 0.25。

如果出现很多错误检测，可以提高到 0.5：

```bat
python predict_image.py --model "D:\模型\best.pt" --source "D:\图片\example.jpg" --conf 0.5
```

如果漏掉很多不明显的小目标，可以尝试降低到 0.15：

```bat
python predict_image.py --model "D:\模型\best.pt" --source "D:\图片\example.jpg" --conf 0.15
```

阈值越低，找到的目标通常越多，但误报也可能变多。

## 12. 常见问题

### `conda` 不是内部或外部命令

关闭当前窗口，从开始菜单打开 Anaconda Prompt 或 Miniconda Prompt，然后重新执行：

```bat
conda activate yolov8
```

### 提示当前环境不是 `yolov8`

执行：

```bat
conda activate yolov8
```

然后再进入项目目录。

### 提示找不到 `ultralytics`

确认激活的是 `yolov8`，然后重新安装本机源码：

```bat
cd /d C:\Users\ASUS\Desktop\ultralytics-main
pip install -e .
cd /d D:\zyzcodex\codex-yolov8
```

这不会复制 Ultralytics 源码到本项目，而是继续使用桌面上的源码目录。

### CUDA 不可用或最小 CUDA 运算失败

不要反复盲目安装不同版本。先记录 `python check_environment.py` 的完整输出。可以临时用 CPU 检查流程：

```bat
python train.py --config config\project.yaml --device cpu --batch 2 --epochs 1
```

CPU 训练会非常慢，只适合排查问题。若要更换 PyTorch/CUDA，建议先克隆或备份现有 Conda 环境，避免破坏当前配置。

### 提示找不到数据集目录

检查 F 盘是否连接、文件夹名称是否完全一致。数据移动后，用记事本修改：

```text
D:\zyzcodex\codex-yolov8\config\project.yaml
```

Windows 路径建议在 YAML 中写成正斜杠，例如：

```yaml
root: "F:/uav-program/训练数据集/Tree-Top-View.v1i.yolov8"
```

### 提示标签坐标或类别错误

错误信息会指出具体标签文件和行号。不要让程序忽略错误继续训练；先备份并检查对应原始标签。合法检测标签每行是：

```text
类别编号 中心点x 中心点y 宽度 高度
```

坐标都必须在 0 到 1 之间。

### 提示输出目录非空

如果是本程序以前生成的派生数据，并且确认可以重建：

```bat
python prepare_dataset.py --config config\project.yaml --overwrite
```

如果程序提示没有安全标记，不要手动绕过保护；先检查路径是否误指向原始数据或重要目录。

### `yolov8n.pt` 下载失败

确认 GitHub 网络可访问，然后重试。也可以手动下载官方 `yolov8n.pt`，放在项目目录，再运行：

```bat
python train.py --config config\project.yaml --model "D:\zyzcodex\codex-yolov8\yolov8n.pt"
```

模型权重已经被 `.gitignore` 排除，不会上传 GitHub。

### 找不到 `best.pt`

先查看训练命令开头显示的输出目录。若训练报错或中途停止，可能只有 `last.pt`；修复问题后用 `--resume` 继续训练。

### GitHub 推送时要求登录

这是 GitHub 身份验证，不影响训练程序。按 Windows Credential Manager 或浏览器提示登录 GitHub。不要把密码、访问令牌写进 Python 文件或提交到仓库。

## 13. 数据质量建议

模型效果上限主要由数据决定。实际无人机图片最好覆盖：

- 不同飞行高度、俯视角度和相机；
- 晴天、阴天、清晨、傍晚和不同季节；
- 大石头、小石块、裸地和相似颜色背景；
- 稀疏树木、密集树冠、遮挡和阴影；
- 没有目标的纯背景图片。

新数据必须人工检查标注。训练集、验证集和测试集之间不要放入同一原图裁剪出的高度相似图片，否则指标可能虚高。

## 14. 未来接入无人机实时画面

第一版没有实现实时界面。后续可以继续使用同一个 `best.pt`，把图片来源换成：

- 本机摄像头编号，例如 `0`；
- 无人机提供的 RTSP/RTMP 地址；
- 本地视频文件。

实时版本还需要处理断流重连、帧率、延迟、录像、线程安全和无人机协议。应在静态图片模型效果稳定后再开发。

## 15. 最短命令清单

每次先执行：

```bat
conda activate yolov8
cd /d D:\zyzcodex\codex-yolov8
```

第一次准备：

```bat
python check_environment.py
python prepare_dataset.py --config config\project.yaml
```

正式训练：

```bat
python train.py --config config\project.yaml
```

继续训练：

```bat
python train.py --resume "实际的last.pt完整路径"
```

识别图片：

```bat
python predict_image.py --model "实际的best.pt完整路径" --source "实际的图片或目录路径"
```

## 16. 数据来源与上传原则

三套原始数据来自 Roboflow，原始 `data.yaml` 标注的许可均为 CC BY 4.0：

- Nature Objects：<https://universe.roboflow.com/s164031-student-dtu-dk/nature-objects/dataset/1>
- Stone 数据集：<https://universe.roboflow.com/finalproject-tclgq/stone-srx04/dataset/1>
- Tree Top View：<https://universe.roboflow.com/treedataset-clsqo/tree-top-view/dataset/1>

GitHub 仓库只保存程序、配置和文档，不保存原始图片、派生数据、训练权重或预测结果。
